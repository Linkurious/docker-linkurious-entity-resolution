apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "linkurious-entity-resolution.fullname" . }}-test-connection"
  labels:
    {{- include "linkurious-entity-resolution.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "linkurious-entity-resolution.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
